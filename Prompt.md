# Prompt

帮我在这个项目下创建一个网页版的提词器，支持输入文本后点击开始按照固定速度/滚动跟随语音识别（重点），支持口播是外接鼠标，在口播滚动的情况下，可以用鼠标上下额外滚动。需要适配手机/平板/电脑尺寸，口播界面支持调整窗口位置、大小（相当于是网页中的一个漂浮窗，能够拖拽调节位置，拖右下角调大小），文字颜色、文字大小、字体，水平翻转/镜像翻转/水平加镜像翻转/90度、180度、270度旋转（及任意角度旋转）

在开始之前，如果你有任何疑问，请先向我提出确认，都了解清楚细节后再开始写代码

---

语音识别采用 浏览器内置 Web Speech API
滚动跟随语音识别实现方式 逐字匹配+自动居中，但有时会有气口，或者个别句子换种表述，需要能够机灵匹配（也就是说需要容忍一部分不匹配情况）
技术栈 Vue + Vite，我已经在当前项目下创建好了Vue空白项目

---

**feat**

- [x] feat: 提词器支持配置
- [x] feat: 鼠标上下滚动时，加个缓动动画，不要直接跳过去，然后每次不要滚太远。不然口播的时候一滚动，可能就找不到读的位置了
- [x] feat: 新增朗读线条，支持上下调整朗读线的位置
- [x] feat: 未开始滚动时，也支持鼠标上下滚动
- [x] feat: 支持配置深色模式/浅色模式/跟随系统
- [x] feat: 支持导出配置(json) 及 导入配置
- [x] feat: 支持本地保存配置避免刷新丢失又需要重新配置，同时添加一个重置按钮可以点击恢复默认设置
- [x] feat: 滚动开始后自动隐藏设置面板 / 停止前如果面板是隐藏状态，停止后则还原面板展示状态，如果停止前面板未隐藏，则不恢复
- [x] feat: 支持全屏模式(屏幕全屏/窗口全屏/浮窗)
- [x] feat: 添加一个配置项，支持勾选英文单词是否断开（勾上之后，如果在一个行的末尾显示不下一个完整的英文单词，就会把这个单词拆开放两行）
- [x] feat: 支持拖入 txt 文档以导入
- [x] feat: 添加 GitHub Action 自动部署到 GitHub Pages 工作流
- [x] feat: i18n 支持 (简体中文、繁体中文、英文)
- [x] feat: 顶部导航栏添加 i18n 切换组件
- [x] feat: 左上角添加 logo
- [x] feat: 支持在左侧配置鼠标滚动一下文字上/下移动多远距离
- [x] feat: 在右侧 可拖拽的提词器浮窗上，添加一个半透明的小拖拽手柄（很细长的一个圆角矩形），让用户一眼就看得出来这个浮窗是支持拖动的
- [x] feat: 支持选择语音识别的语言
- [x] feat: 语音识别时，加载需要一定时间，添加一个 loading 动画，让用户知道正在加载中
- [x] feat: 添加一个配置项，支持选择是否移除连续空白行，默认勾选（没处换行位置最多保留一个空白行）
- [x] feat: 支持调整文字间距 / 行距

**fix**

- [x] fix: 旋转时朗读线条没有跟着旋转
- [x] fix: 右上角 设置/隐藏设置没有效果
- [x] fix: 语音跟随没效果 - 语音跟读好像有 bug, 点开始后网页会卡一下, 但之后不管怎么读, 都没有跟着, 一直不动 / 后来修了一下，好像说很长一句话之后，可以动一下，但是是随机跳到一个位置，不是实际读到的位置
- [x] fix: 滚动时，鼠标上下滚动增益有时候没反应，以后时候先向下滚，然后再向上滚到开始的位置，不能继续往上滚动
- [x] fix: 滚动到最后的时候，最后有一些文字没有过线就结束了，应该所有文字都过线才结束 / 滚动开始时，鼠标滚动到最上面，最开始的那部分文字不能完全到线下面
- [x] fix: 恢复默认设置时并没有把所有状态都重置 大部分状态都还没有被重置回默认设置 几乎没有效果
- [x] fix: 英文单词可在行尾断开（拆开放两行）配置项未生效
- [x] fix: 朗读线条未正确显示（百分比配置也不对：500% - 9500%）
- [x] fix: 当页面过窄，比如在手机端上看的时候，只能看到左边的配置页，顶部的导航栏和提词器的部分都不见了
- [x] fix: 部分 i18n 翻译丢失，请过一下代码，帮我补充丢失的翻译
- [x] fix: 语音跟读 第一句话前半句逐字能跟读，但是之后就不动了，不知道是跟不上了还是什么其他的原因 (会不会是result chunk 拼接的原因 你能将相关的位置打印一些好识别的日志出来，我们一起来判断一下吗？) (F12中已经看到很多 onresult 事件的回调打印，但是页面上并没有滚动)
- [x] fix: 页面刚加载完的状态，应该是提词器文案在朗读线下面，而不是在提词器浮窗的顶部位置
- [ ] fix: 语音识别模式下，提词器右上角的 [暂停] 似乎没用，暂停时，说话依然能往后匹配
- [ ] fix: 目前这个功能在电脑端 Chrome 浏览器中正常运行，但在安卓手机和安卓平板的 Chrome 中，语音识别好像不能正常工作 (Tab 标签页的那个录音标志在狂闪)
- [ ] fix: 语音识别过程中报错 network 后重连，继续说话能够识别到说话内容，但是文字不会继续往后走

**refactor**
- [ ] refactor: 将代码中的注释全部写成 English+中文 的形式，英文在前，如果允许，中英文最好不写在一行（如果是行内注释，可以例外），方便阅读

**docs**
- [x] docs: 以使用该项目的实际用户角度为主，站在产品经理角度（而非工程开发角度），更新 README.md +  README.zh-CN.md
 > 需要满足：让用户一眼就知道这给项目是用来做什么的，并给出 GitHub 仓库地址 & 演示地址
 > 尽可能完整的列出项目功能，亮点功能放在前面突出展示

---

## 反复使用到的 Prompt

以上每一个改动都在本地形成一个提交，记得把你自己加为该提交的 co-author。确保每一个提交都是原子性，可运行的 (仅提交你改动的文件，本地的其他改动不要提交) (Git 提交时，提交信息应使用英语，格式 `<type>(<scope>): <subject>`)
CodeBuddy: `Co-authored-by: CodeBuddy <codebuddy@tencent.com>`

以上改动在本地形成一个提交，记得把你自己加为该提交的 co-author (仅提交你改动的文件，本地的其他改动不要提交) (Git 提交时，提交信息应使用英语，格式 `<type>(<scope>): <subject>`)
CodeBuddy: `Co-authored-by: CodeBuddy <codebuddy@tencent.com>`

帮我将本地 Git 暂存区中的文件提交到代码仓库 (仅提交暂存区的文件，本地的其他改动不要提交) (Git 提交时，提交信息应使用英语，格式 `<type>(<scope>): <subject>`)

代码提交后，请分别使用 `git push` 和 `git push -u github` 推送到多个远端仓库

docs: 更新 README.md 和 README.zh-CN.md 文档以匹配最新文档
以使用该项目的实际用户角度为主，站在产品经理角度（而非工程开发角度）更新 README.md + README.zh-CN.md
尽可能完整的列出项目功能，亮点功能放在前面突出展示
这个项目代码由人和AI共同完成，项目根目录下的Prompt.md 详细记录了大部分功能及开发时遇到的一些重点问题，可以参考，但以项目代码实现的实际功能为准
如果你觉得现有 README 中有比较好的表述，请保留下来

---

## 重点问题

### 提醒用户选择正确语言

- [x] feat: 浏览器只能按照选择的语言来去识别，所以语音识别开始前提醒要在UX交互设计上提醒用户选择正确语言，并且在页面上提醒用户，每次只能读同一种语言，浏览器侧不支持多种语言比如中文+英文混合识别 开始之前，请简要给出你的方案

AI:
```
**1. 开始前交互提醒（选择正确语言）—— 启动确认弹窗**
- 点击「开始」且当前为「语音跟随」模式时，不直接启动，而是先弹出确认对话框：
  - 标题：请确认语音识别语言
  - 展示当前所选语言（如「简体中文（普通话）」）
  - 提示：浏览器只能按所选语言识别，开始后请全程使用该语言口播；**不支持中/英文等混合语言识别**。
  - 按钮：`确认并开始` / `取消`
- 用户在弹窗中可一眼确认语言正确与否，点「确认并开始」才真正 `speech.start(...)`。
- 把 `recLangs` 列表抽成共享 `src/utils/recLangs.ts`（含 `recLangLabel()`），`ControlPanel` 与 `App.vue` 共用，避免语言标签重复定义。

**2. 页面常驻提醒（每次只能读同一种语言）—— 两处**
- **设置面板内**：语音模式下，语言选择器下方增加一行常驻提示：「注意：每次仅能识别单一语言，不支持中英文等混合识别。」
- **提词器浮窗内**：语音口播进行中（面板已隐藏），在浮窗顶部常驻一条细横幅，显示「当前仅识别「简体中文」单一语言，请全程用该语言口播（不支持混合识别）」。停止/非语音模式时不显示。
```

1. 开始前交互提醒（选择正确语言）—— 启动确认弹窗

我觉得可以这样，就是在用户输入的那一段文本，我们可以判断它是不是多语言混合。(当然，你说的考虑到它可以允许有个别字词混进去，但是大段的不行。)我们可以根据用户输入的是否有大段的不一致的语言，如果有则弹窗提醒用户【每次仅能识别单一语言，不支持中英文等混合识别。】
然后，如果用户输入的文本语言与识别下拉框所选语言不一致，则弹窗提醒用户选择正确的语言（【返回选择】高亮按钮，【继续开始】弱化按钮）

如果说用户输入的和选择都没有问题，其实我们不需要单独的再弹窗提醒。

2. 页面常驻提醒（每次只能读同一种语言）

设置面板中加一下就好，提示器浮窗内就不加了

第1点识别那里，可能会涉及到一些算法逻辑，最好能够封装成函数，便于后期维护

---

- [x] feat: 在检测到文稿为混合语言、识别语言可能不一致 弹窗中展示判断出的各语言百分比

下面这段话，识别的好像不太准（英文应该是按一个单词来算，标点符号不统计）

### 识别内容处理方案

fullText 匹配现在是拿着一句话的全部内容去匹配的，还是跟上一个识别result 做 diff, 拿到增量的那部分去匹配的？

### 多语言混合识别可行性

- [ ] feat: 是否支持多语言混合语音识别？ 比如一段是中文，一段是英文这种

(浏览器原生不支持，或许可以考虑一下双通道识别)

### 浏览器识别相关问题

本地浏览器的 Web Speech API 需要请求网络吗？比如 Edge 和 Chrome 是否需要请求它们对应的网络服务？在长语音识别场景下，如果连续说话时间较长，会不会有问题？因为我们之前有一个 result 字符串，它会返回所说的全部内容。说话时间越长，返回的响应内容就会越来越大。这里会不会容易有问题？
